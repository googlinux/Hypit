# Studio 设置与 API Key 管理

## 打开页面

在仓库根目录运行：

```bash
node bin/hypit.mjs studio --settings
```

打开终端显示的 Settings 地址（默认 `http://127.0.0.1:5179/#settings/api-keys`）。
此模式不需要 `.svrun` 文件，也不加载视频编辑器。已经在编辑视频时，可直接点击右上角 **设置**，返回编辑器后继续工作。

指定已有运行环境：

```bash
node bin/hypit.mjs studio --settings --runtime /path/to/hypit.runtime.json
```

不传 `--runtime` 时，使用当前项目的 `.hypit/runtime` 选择。初次使用可先执行 `node bin/hypit.mjs runtime init`，然后启动 Studio。也可以用 `runtime use <profile>` 选择已有配置。设置页不创建或改写运行环境文件；变更运行环境选择后需要重启 Studio。

安装版命令去掉 `node bin/hypit.mjs`，改为 `hypit` 即可。

## 页面功能

| 页面 | 功能 |
| --- | --- |
| 通用设置 | 中英文及已加载语言包切换、跟随系统或减少动画、恢复默认编辑器面板尺寸 |
| API Key | 显示当前 Provider 的凭据状态，保存新密钥、确认替换已有凭据、确认删除、刷新状态 |
| 运行环境 | 查看项目目录、运行环境配置路径、系统与 Node.js 版本、Provider 包及配置中的服务来源地址 |

页面适配桌面和手机尺寸；通用偏好在当前浏览器中自动保存。恢复布局会重新加载页面，只清除面板尺寸设置。

## 管理密钥

1. 在服务商控制台创建对应的 API Key。
2. 在 **API Key** 页面找到 Provider，输入密钥并保存。
3. 状态变为 **已配置** 后，新任务会使用该凭据。这个状态只说明存储中存在凭据，不代表服务商已验证密钥或账户额度。

默认运行环境使用 HypiHub，多个模型通过该 Provider 调用；本页不会凭空添加 OpenAI、Anthropic 等未配置的 Provider。自定义运行环境会按实际声明列出凭据槽位。

默认 `os` 存储使用 macOS Keychain / Windows 凭据库；浏览器不会接收已保存密钥的值。密钥通过受保护的本地接口提交，输入框在提交或离开页面时清空，不保存到浏览器偏好或项目 JSON。保存接口复用 CLI 的凭据管理逻辑。

**替换操作也会覆盖同一槽位中的 OAuth 登录凭据。** 共用同一存储与凭据引用的其他项目也会受到替换或删除影响，页面会要求确认。

环境变量存储、不可写存储、结构化 JSON 凭据仅显示状态，使用对应环境配置或 CLI 管理。Linux 尚不支持默认 OS 凭据库，可配置现有的环境变量凭据存储。

## 出错时

- **尚未选择运行环境**：创建或选择运行环境后重启 Studio，或传入 `--runtime`。
- **无法读取凭据**：检查配置是否有效、Provider 是否安装、系统凭据库是否允许访问。
- **保存/删除失败**：先刷新确认当前状态，再检查权限并重试。失败后不会保留输入框里的密钥。
- **配置状态正常但生成失败**：检查服务商的密钥权限、账户额度和模型支持情况。本页面不会发起付费生成或主动测试远程连接。

## 接口与安全边界

- 设置接口仅使用启动时选定的运行环境，HTTP 请求不能指定任意配置文件或凭据库路径。
- 写操作需要精确匹配的本地 Host、同源 Origin、会话 token 和 JSON Content-Type。
- 仅允许操作该运行环境实际声明的 Endpoint / 凭据槽位。请求体上限 32 KiB，密钥上限 16 KiB，拒绝空密钥、NUL 和换行。
- 返回值只包含配置元信息及状态，不回传密钥、不回传底层异常内容、不打印提交的密钥。响应禁用缓存。
- 服务地址只展示 `baseUrl` 的来源（协议、主机、端口），去除用户名、密码、路径与查询参数；它不是完整外联清单。完整连接分析见 [专项检查](research/2026-09-16/network-inventory.zh-CN.md)。
- Studio 继续只监听本机回环地址。本页不是面向公网的多用户后台。

## 验证

新增 HTTP 集成测试覆盖保存、替换、删除、缺少确认、跨站/缺少 token、只读存储、未声明槽位、请求大小、秘密脱敏和真实运行环境声明读取。

使用内置浏览器验证中英文切换、偏好重载、恢复布局确认、API Key 保存/替换/删除/取消及状态刷新，并检查桌面和手机布局。写入流程使用隔离的内存测试凭据，不覆盖现有用户凭据；没有调用服务商进行真实密钥验证或付费生成。

### 2026-09-16 验证记录

- `tsc -p tsconfig.json --noEmit` 通过。
- Node 26.4.0 默认全量测试：864 项，836 通过、0 失败、28 跳过。
- 项目指定 Node 24.14.1 串行全量测试：同样 836 通过、0 失败、28 跳过。
- Node 24 的并行运行出现原有 `cancelling Surface validation stops both probes without poisoning later calls` 两秒进程启动等待超时；定向及串行复测通过。未修改这个既有测试或业务逻辑。
- 跳过项依赖尚未安装的 FFmpeg、图像服务或完整渲染环境；不代表完成了真实视频导出验证。
- 内置浏览器：1536 × 1024 桌面、390 × 844 手机；设置页无横向溢出，页面标题正确，无空白页、框架错误遮罩或相关控制台错误。
- 交互闭环：语言切换与重载、减少动画、布局恢复、保存测试密钥、确认替换、取消删除、确认删除、刷新状态、返回编辑器、进入设置暂停播放。
- 集成检查使用本地纯色 Film 测试 Run，不依赖远程模型；API Key 写入检查使用独立内存存储。

视觉采用 Studio 现有深色与玫红色系，设计稿和浏览器截图逐项检查了导航、内容布局、标题层级、配色、表单边框与间距、手机响应式布局。修正了隐藏按钮被样式覆盖以及桌面内容间距偏紧的问题。为兼容现有 Studio 保留原顶部导航与图标体系；独立设置模式省略编辑器入口，增加真实刷新/确认/错误状态，并使用准确的凭据状态说明。截图中的项目名称来自隔离测试环境。
